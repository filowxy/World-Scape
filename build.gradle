plugins {
    id 'net.neoforged.moddev' version "$moddevVersion"
    id 'org.jetbrains.kotlin.jvm' version "$kotlinVersion"
}

repositories {
    maven {
        name = 'Kotlin for Forge'
        url = 'https://thedarkcolour.github.io/KotlinForForge/'
    }
}

neoForge {
    version = "$neoForgeVersion"

    mods {
        worldscape {
            sourceSet sourceSets.main
        }
    }

    // Mixin configuration for NeoForge 21.1.219
    runs {
        client {
            client()
        }
        server {
            server()
        }
    }

    // Access Transformer for exposing final fields
    accessTransformers = files('src/main/resources/META-INF/accesstransformer.cfg')
}

dependencies {
    // Kotlin for Forge — language provider for Kotlin-based mod code
    implementation "thedarkcolour:kotlinforforge-neoforge:$kffVersion"

    // Mixin annotation processor
    compileOnly "org.spongepowered:mixin:$mixinVersion"
    annotationProcessor "org.spongepowered:mixin:$mixinVersion:processor"

    // Test dependencies
    testImplementation "org.junit.jupiter:junit-jupiter:$junitVersion"
    testRuntimeOnly 'org.junit.platform:junit-platform-launcher'
}

test {
    useJUnitPlatform()

    // Include NeoForge/Minecraft dependencies on the test classpath.
    // ModDev 2.0 injects Minecraft via the main sourceSet runtimeClasspath
    // but does not automatically extend the test classpath with it.
    classpath = sourceSets.main.runtimeClasspath + sourceSets.test.runtimeClasspath
}

// Kotlin-Java interop: ensure Java compiler sees Kotlin output
// Kotlin-Java 互操作：确保 Java 编译器能访问 Kotlin 编译产物
afterEvaluate {
    tasks.named('compileJava').configure {
        dependsOn compileKotlin
        classpath += files(tasks.named('compileKotlin').get().outputs.files)
    }
}
