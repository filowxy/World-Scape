# World Scape 项目宪章

## 项目核心定位

World Scape 是一个 Minecraft NeoForge 1.21.1 地形生成模组。它**完全替换**原版地形生成系统，提供一套基于数学函数的地形生成框架。

**核心目标**：为整合包作者和玩家提供**极致兼容、可配置、高性能**的地形生成基础设施。

---

## 设计哲学

### 第一原则：兼容性优先

World Scape 的核心竞争力不是“最好的算法”，而是**最稳定的兼容性**。

- 地形与生物群系**完全解耦**。World Scape 只负责生成地形骨架（石头等基础方块），生物群系、结构、植被等完全交由其他模组处理。
- 不修改结构生成逻辑、不添加新方块、不添加新实体。
- 所有设计决策必须优先考虑与 C2ME、Distant Horizons、TerraBlender 等主流模组的共存能力。
- 为兼容性可以适度牺牲算法精度或视觉效果。

### 第二原则：地形即函数

v4.1 核心哲学：29 种地形类型各自拥有独立的、基于真实地质过程的数学函数签名。

- 每种地形类型的数学签名已从硬编码迁移到 JSON 配置（`defaults.json`）。
- 函数由 `TerrainFunctionInterpreter` 解释执行，支持通过数据包扩展。
- 控制点类型和偏移量由 `TerrainFieldSampler` 的连续噪声场（能量场+湿润度场）驱动，确保跨区域一致性。

### 第三原则：宏观-微观两层架构

- **宏观层**：Voronoi 图划分大陆框架（2048格单元），决定海拔等级（Tier 0-5）和基准高度。
- **微观层**：连续噪声场驱动控制点（512格区域），在宏观约束下生成局部地形细节。
- 两层通过 `RegionController.calculateBlend()` 的平滑混合机制整合。

---

## 关键设计约束

### 必须遵守的硬性限制

1. **不引入新噪声场**：所有地形生成必须基于 `TerrainFieldSampler` 已有的噪声函数。
2. **种子确定性**：相同种子必须生成完全相同的地形，不引入独立随机种子。
3. **不修改核心数学签名**：`TerrainCalculator.calcHeightForType` 的函数签名不得变更。
4. **JSON 配置向后兼容**：`defaults.json` 的 schema 变更必须兼容旧版本配置。

### 已知的技术债务

1. **悬崖问题**：Voronoi 边界处的高度跳变经过多次修复（`sqrt` 替换 `smoothstep`、`tierGapFactor`、移除 `BLEND_WEIGHT_THRESHOLD` 硬切换），但可能仍有残留。根因可能不是单个算法问题，而是多个子系统的交互效应。

2. **Tier 5 覆盖率极低**：当前 Tier 5 原始概率约 6.67%，叠加邻居修正后几乎全部被拉低。HIGH_MOUNTAINS 等高海拔地形在大多数种子中缺失。

3. **回退机制不兼容**：项目存在多个独立的回退路径（`defaults.json` 解析失败、`ReflectionSurfaceAdapter` 降级、`TerrainBiomeRules` 白名单为空），但这些回退触发后下游模块可能不知情，导致数据不一致。

4. **种子分析器正确性**：分析器曾因未加载地形函数定义而输出错误结果。需要确认修复后分析器与游戏内生成完全一致。

---

## 绝对禁止

以下行为在任何情况下不得执行：

- `git rm -f`、`git reset --hard`、`git clean -fd`、`git checkout -f`
- 使用 PowerShell 或命令行直接写入 Java 文件内容
- 在未与用户确认的情况下删除任何文件
- 修改 `.git` 目录下的任何内容
- 在核心地形生成逻辑中引入新的噪声场或随机源

---

## 协作准则

- 所有 AI 协作模型在开始任何任务前，必须先阅读本文件和 `memory.md` 了解项目历史和当前状态。
- 遇到多方案选择时，必须停止并询问用户，不得自行决定。
- 所有代码修改必须通过 `gradlew build` 验证编译通过。
- 所有新增或修改的代码必须包含中英双语注释。