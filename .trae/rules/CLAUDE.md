1. 网络搜索总结这个agent很好用，可以帮助你搜索有关neoforge和我的世界的API

2. 记忆管理规范（强制执行）

在与开发任务交互时，需将我明确要求记住或你认为至关重要的信息，还有你通过搜索获取到的一些关键知识，按以下标准分级，并更新至 `memory.md`。

3. 记忆等级标准
- **🔴 P0 - 致命规则，你觉得最最重要的要记录的事情或是知识**
- **🟡 P1 - 关键决策，你觉得比较重要的要记录的事情或是知识**
- **🟢 P2 - 经验教训，你觉得有必要记录但不那么重要的事情或知识**
4. 排序与格式规范
    - 所有记忆条目必须按 P0 → P1 → P2 的**等级**排序。
    - 同一等级内，按**时间倒序**排列。
    - 每条记忆必须遵循固定格式：
    - **[P等级] YYYY-MM-DD | 类型(决策/Bug/偏好)**
    - **触发场景**: 简短描述。
    - **核心内容**: 具体是什么。
    - **永久准则**: 提炼出的必须遵守的规则。
    
**注意：搜索到的东西要确保正确再记录。**

5. memory.md是给你进行记忆的，不是给用户看的，你要确保你自己能够理解！！！

6. **不要嫌弃方案复杂，只要方案够好就行，不要担心时间太长！！！** 。

7. **不要嫌弃方案复杂，只要方案够好就行，不要担心时间太长！！！** 。

8. **不要嫌弃方案复杂，只要方案够好就行，不要担心时间太长！！！** 。 

9. **绝对准则：**
    1. 所有方案都必须符合项目需求。
    2. 所有方案都必须符合项目性能要求。
    4. 所有方案都必须符合项目可维护性要求。
    5. 所有方案都必须符合项目可扩展性要求。
    6. 所有方案都必须符合项目可监控性要求。
    7. 所有方案都必须符合项目可测试性要求。
    8. 所有方案都必须符合项目可监控性要求。
    9. **不要担心时间太长！！！** 。
    10. **仔细！仔细！仔细！**
10. 不允许修改memory.md文件，只允许添加记忆条目。

11. **版本控制最佳实践（强制执行）**
    - **破坏性操作前必须先备份**：修改文件前运行 `git status` 确认状态，至少 `git add` + `git commit` 确保有可回退点。无 commit 历史时，手动复制 `src/` 目录到临时备份。
    - **创建分支执行关键变更**：重构、大规模重写或删除操作前，创建独立分支：`git checkout -b feature/<name>`。
    - **定期提交**：每个逻辑变更完成后立即提交，不累积改动。
    - **提交信息规范**：使用 Conventional Commits 格式：`<type>(<scope>): <description>`。类型包括 feat/fix/refactor/perf/chore/docs。
    - **执行重大操作前**：运行 `git stash list` 确认无悬空 stash，运行 `git status --porcelain` 确认工作区干净。